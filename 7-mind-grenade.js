const num1 = 5;
const num2 = 10;

const addValue = () => {
  console.log(`the sum is ${num1 + num2}`);
};

//if we have a function that we invoke then the `require` will call this function
addValue();
